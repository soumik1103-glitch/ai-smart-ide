
import { localize } from '../../../nls.js';

const open = ( localize(1956, 'open'));
const close = ( localize(1957, 'close'));
const find = ( localize(1958, 'find'));
var localizedStrings = {
    open: open,
    close: close,
    find: find
};

export { localizedStrings as default };
